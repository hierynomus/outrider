apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "outrider.serviceAccountName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "outrider.serviceAccountName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "outrider.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
